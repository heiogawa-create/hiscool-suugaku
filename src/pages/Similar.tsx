import{useState}from'react';import{Button,Card,Page}from'../components/common';import{useApp}from'../context/AppContext';
export default function Similar(){const{currentProblem}=useApp();const[done,setDone]=useState(false);return <Page title="類題に挑戦"><Card><label>難易度<select><option>やさしい</option><option>同じレベル</option><option>少し難しい</option><option>入試レベル</option></select></label><p>{currentProblem.similar[0].text}</p><label>回答形式<select><option>数値</option><option>数式</option><option>選択式</option><option>記述式</option></select></label><textarea placeholder="解答を入力"/><Button onClick={()=>setDone(true)}>回答する</Button>{done&&<div className="ok"><b>考え方は合っています</b><p>計算ミスに注意。{currentProblem.similar[0].explanation}</p><p>次のおすすめ：同じ単元をもう1問。</p></div>}</Card></Page>}
