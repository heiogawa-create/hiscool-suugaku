import{useState}from'react';import{Link}from'react-router-dom';import{Button,Card,MathText,Page}from'../components/common';import{useApp}from'../context/AppContext';import{createReviewItem}from'../services/reviewService';
export default function Solution(){const{currentProblem,addReview}=useApp();const[u,setU]=useState('');const s=currentProblem.solution;return <Page title="解説・理解確認"><Card><h2>問題のポイント</h2><p>{s.point}</p><h2>使用する考え方</h2><p>{s.approach}</p>{s.steps.map(x=><p><b>{x.label}</b> <MathText text={x.formula}/> — {x.explanation}</p>)}<h2>答え</h2><p>{s.answer}</p><p>{s.why}</p><h2>間違えやすいポイント</h2><ul>{s.pitfalls.map(p=><li>{p}</li>)}</ul><p>別解: {s.alternative}</p><p>関連公式: {s.relatedFormula}</p><p>次に復習: {s.nextReview}</p></Card><Card><h2>理解度</h2>{['よく分かった','だいたい分かった','まだ少し不安','分からなかった'].map(x=><label className="check"><input type="radio" name="u" onChange={()=>setU(x)}/>{x}</label>)}<p>{u.includes('不安')||u.includes('分から')?'もう一度やさしく説明・前の単元に戻るのがおすすめです。':'類題に挑戦しましょう。'}</p><Button onClick={()=>addReview(createReviewItem(currentProblem,5))}>間違いノートに保存する</Button><Link to="/similar"><Button>類題に挑戦する</Button></Link></Card></Page>}
