import{useState}from'react';import{useNavigate}from'react-router-dom';import{Button,Card,Loading,Page}from'../components/common';import{recognizeProblem}from'../services/imageRecognitionService';import{useApp}from'../context/AppContext';
export default function Input(){const[preview,setPreview]=useState('');const[text,setText]=useState('');const[err,setErr]=useState('');const[loading,setLoading]=useState(false);const{setRecognized}=useApp();const nav=useNavigate();async function run(){if(!preview&&!text){setErr('画像を選ぶか、問題文を入力してください。');return}setLoading(true);setRecognized(await recognizeProblem(text));nav('/confirm')}return <Page title="問題を撮る・入力する">{loading&&<Loading/>}<Card><p>教科書・問題集・プリント・ノート・手書きの途中式・グラフ・表・図形に対応予定です。</p><p className="notice">問題集や教科書の画像は、個人の学習目的で使用してください。</p><input aria-label="カメラで撮影" type="file" accept="image/*" capture="environment" onChange={e=>setPreview(e.target.files?.[0]?URL.createObjectURL(e.target.files[0]):'')}/><input aria-label="画像をアップロード" type="file" accept="image/*" onChange={e=>setPreview(e.target.files?.[0]?URL.createObjectURL(e.target.files[0]):'')}/>{preview&&<img src={preview} alt="選択した問題画像のプレビュー" className="preview"/>}<label>問題文を直接入力<textarea value={text} onChange={e=>setText(e.target.value)} placeholder="例：y=x^2-4x+1 の頂点を求めよ"/></label>{err&&<p className="error">{err}</p>}<ul><li>問題全体が入るように撮影</li><li>影や反射を避ける</li><li>文字が読める明るさで撮影</li></ul><Button onClick={run}>AIで問題を読み取る</Button></Card></Page>}
