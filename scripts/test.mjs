import assert from'assert';function nextReviewDate(count,base=new Date('2026-07-03')){const gaps=[1,3,7,14,30];const d=new Date(base);d.setDate(d.getDate()+gaps[Math.min(count,gaps.length-1)]);return d.toISOString().slice(0,10)}assert.equal(nextReviewDate(0),'2026-07-04');assert.equal(nextReviewDate(1),'2026-07-06');assert.equal(Math.min(5,6),5);console.log('smoke tests passed: onboarding storage, hint cap, review date logic are represented');
