import http from'http';import{readFileSync,existsSync}from'fs';import{extname,join}from'path';const port=5173;http.createServer((req,res)=>{let p=req.url==='/'?'/index.html':req.url.split('?')[0];let file=join(process.cwd(),p==='/index.html'?'index.html':p.slice(1));if(!existsSync(file))file='index.html';res.setHeader('content-type',{'.js':'text/javascript','.css':'text/css','.html':'text/html'}[extname(file)]||'text/plain');res.end(readFileSync(file))}).listen(port,()=>console.log(`http://localhost:${port}`));
